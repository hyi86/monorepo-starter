# Code Highlight in MDX

### Line, Word Highlight

```tsx {1} /Clicked/ fileName="button.tsx"
import React from 'react';

export default function Button({ label }: { label: string }) {
  return (
    <button onClick={() => alert('Clicked')} className="rounded bg-blue-500 px-4 py-2 text-white">
      {label}
    </button>
  );
}
```

### Line Diff

```ts
function add(a: number, b: number) {
  return b + b; // Remove this line [!code --]
  return a + b; // Add this line [!code ++]
}
```

### Hide line numbers

```yaml fileName="config.yaml" showLineNumbers=false
a: 1
b: 2
c: 3
case: |
  멀티라인
  티
  라
  인    스트링
```
