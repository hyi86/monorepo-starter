import { addons } from 'storybook/manager-api';

/**
 * @see {@link https://storybook.js.org/docs/configure/user-interface/theming#create-a-theme-quickstart}
 */
addons.setConfig({
  showPanel: true,
  panelPosition: 'bottom',
});
